name: lyso-md
channels:
  - conda-forge
channel_priority: strict
dependencies:
  - python=3.11
  - pip
  - pyyaml>=6.0,<7
  - pydantic>=2.7,<3
  - typer>=0.12,<1
  - jinja2>=3.1,<4
  - pytest>=8,<10
  # Planned later-phase Python dependencies. Amber itself comes from the HPC module.
  - numpy>=1.26,<3
  - scipy>=1.12,<2
  - networkx>=3.2,<4
  - rdkit>=2024.03
  - gemmi>=0.6,<1
  - parmed>=4.2,<5
  - pip:
      - -e .
